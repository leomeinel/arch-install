#!/usr/bin/env bash

# Fail on error
set -e

# Execute flatpak
# FIXME: Currently unverified; See: https://gitlab.gnome.org/GNOME/file-roller/-/issues/284
exec /usr/bin/flatpak run org.gnome.FileRoller "${@}"
